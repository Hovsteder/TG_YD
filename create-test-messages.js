const { Pool } = require('pg'); async function main() { try { const pool = new Pool({ connectionString: 'postgres://telegram_user:telegram_secure_pwd@localhost:5432/telegram_app' }); console.log('Connected to database'); const query = ; const result = await pool.query(query); console.log('Inserted messages:', result.rows.length); await pool.end(); } catch (err) { console.error('Error:', err); } } main();
